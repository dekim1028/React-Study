let jwt;_e40‍.x([["default",()=>_e40‍.o]]);_e40‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const jwtMiddleware = (ctx, next) =>{
    const token = ctx.cookies.get('access_token');
    if(!token) return next(); //토큰이 없음
    try{
        const decoded = jwt.verify(token,process.env.JWT_SECRET);
        _e40‍.g.console.log(decoded);
        return next();
    }catch(e){
        //토큰 검증 실패
        return next();
    }
};

_e40‍.d(jwtMiddleware);